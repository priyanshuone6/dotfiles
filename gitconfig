[user]
    name  = Kapil Agarwal
	email = kapilag96@gmail.com
[alias]
    tree = log --decorate --graph --oneline
    pick = cherry-pick
    logs = log --format='%C(yellow)%h%C(reset) %C(cyan)%<(32)%an%C(reset) %ad %s' --date=short
    rb   = "!f() { if (($# != 0)); then git rebase --interactive --autosquash HEAD~$1; else git rebase --interactive --autosquash; fi;}; f"
    co   = checkout
    merge= merge --squash
[branch]
	autosetuprebase = remote
[core]
	excludesfile = gitignore_global
	editor = vim
[help]
	autoCorrect = -1

